[package]
name = "pvxs-sys"
version = "0.1.2"
edition = "2021"
authors = ["Tine Zata <https://github.com/TineZata>"]
description = "Low-level FFI bindings for EPICS PVXS library"
license = "MPL-2.0"
repository = "https://github.com/TineZata/pvxs-sy"
keywords = ["epics", "pvxs", "rust", "c++", "wrapper"]
categories = ["external-ffi-bindings", "development-tools", "science"]
readme = "README.md"
documentation = "https://docs.rs/pvxs-sys"
homepage = "https://github.com/TineZata/pvxs-sys"

# This is a *-sys crate providing low-level FFI bindings
links = "pvxs"

[dependencies]
cxx = "1.0.194"
tokio = { version = "1.50.0", features = ["full"], optional = true }
futures = { version = "0.3.32", optional = true }
chrono = { version = "0.4.44", features = ["serde"] }
crossbeam-channel = "0.5"

[features]
default = ["async"]
async = ["tokio", "futures"]

[build-dependencies]
cxx-build = "1.0.189"

[dev-dependencies]
serial_test = "3.4.0"

[lib]
name = "pvxs_sys"
path = "src/lib.rs"

[package.metadata.docs.rs]
# Docs.rs doesn't have EPICS/PVXS installed, so skip actual compilation
# Documentation will be generated from Rust source only
rustdoc-args = ["--cfg", "docsrs"]
no-default-features = true
features = []
